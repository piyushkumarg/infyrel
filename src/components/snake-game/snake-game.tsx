import { SiteLayout } from '@/layout';

const SnakeGame = () => {
  return (
    <SiteLayout className="bg-slate-300 min-h-[90vh]">
      <div>Snake Game</div>
    </SiteLayout>
  );
};

export default SnakeGame;
