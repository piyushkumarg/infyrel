import { SiteLayout } from '@/layout';
import React from 'react';

const Header = () => {
  return (
    <SiteLayout className="bg-slate-800 p-2">
      <div className="max-w-screen-2xl w-full text-white">Navbar</div>
    </SiteLayout>
  );
};

export default Header;
